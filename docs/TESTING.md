# Testing

The stable gate includes Python protocol tests, C# regression groups, full synthetic IQ golden-vector decoding, exact SDR# ABI checks, SQLite/JSONL integration, five-minute soak smoke and a mandatory 24-hour release soak.
